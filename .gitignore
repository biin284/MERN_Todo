# Node modules for both client and server
node_modules/
client/node_modules/
server/node_modules/

# Environment variables (sensitive information)
.env
client/.env # If you have a separate .env for client (though usually VITE_ vars are used in client)
server/.env

# Build output
client/dist/
server/build/
server/dist/ # Common for some Node.js builds

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS and IDE specific files
.DS_Store
.vscode/